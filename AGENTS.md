# Music Server & App

A private, self-hosted music platform: a remote server hosts a lossless/downloaded music library and streams it to a cross-platform client app. The experience targets parity with apps like [Poweramp](https://powerampapp.com/) and Spotify, but self-owned — the server is the authority when online, and the client falls back to its offline cache when the server is unreachable.

## Repository Layout
- **[`server/`](./server/AGENTS.md)** — Rust backend. gRPC primary interface, REST fallback. Hosts the library, auth, streaming, ingest, recommendations, audit log. See [`server/AGENTS.md`](./server/AGENTS.md).
- **[`app/`](./app/AGENTS.md)** — [Tauri 2](https://tauri.app/) client (React 19 + TypeScript frontend, Rust native core). Cross-compiles to desktop + Android (iOS best effort). See [`app/AGENTS.md`](./app/AGENTS.md).

## Core Features
- **Streaming** of lossless/downloaded music from server to client.
- **Online/offline authority:** server is source of truth when reachable; client uses offline cache otherwise.
- **Recommendations** via fingerprint matching.
- **Playlists:** create / update / delete.
- **Offline use:** download and archive content for playback without the server.
- **Album artwork:** fetched automatically.
- **Metadata editing:** optional, manual, opt-in — both on upload and after upload completes.
- **Uploads:** push new music from clients individually or as archives (archive file, ISO, CD, and other popular music archiving formats).
- **Ingest folder:** a server-side folder where placed files (e.g. completed torrents) are **copied — not moved** — out and auto-organised into the library. Source files are never altered or removed.
- **Follow + notifications:** users follow artists and get notified of new releases.
- **Audit log + rollback:** every change is logged, viewable by managers/admins, and rollback-able (e.g. metadata edits).

## Architecture

```
┌─────────────────────────┐         gRPC (primary)          ┌──────────────────────────┐
│   Client App (app/)      │ ───────────────────────────────▶│   Server (server/)        │
│  Tauri 2 + React + TS    │         REST  (fallback)        │   Rust · gRPC + REST      │
│  desktop / Android / iOS │ ◀───────────────────────────────│   library · auth · ingest │
│  offline cache fallback  │                                 │   audit log · streaming   │
└─────────────────────────┘                                 └──────────────────────────┘
```

- **Transport:** gRPC is the primary interface; REST is a feature-parity fallback. Both share the same auth and permission enforcement. The client mirrors this gRPC-primary/REST-fallback preference.
- **Authority model:** server wins when online; client degrades gracefully to its offline cache.
- **Native work** (filesystem cache, secure credential storage, downloads) lives in the client's Rust side via `#[tauri::command]`; React handles UI/state.

## Authentication & Authorization
Both gRPC and REST use the same auth. Two mechanisms:
1. **`SECRET_KEY`** environment variable — pre-shared key. Changeable at runtime via the admin UI or by an authenticated admin client.
2. **Username/password** accounts — multiple accounts with tiered permissions.

Permission levels, each inheriting the level below:
- **Admin:** modify everything, including other accounts' permissions and levels.
- **Manager:** manage the music library (create, update, delete, change metadata, etc.). Cannot change settings or other users.
- **User:** read-only. Can still download and archive for offline use.

Permission checks are **enforced server-side** on every endpoint; the client only gates UI affordances.

## Server (summary)
Rust (edition 2024). gRPC primary, REST fallback. Owns library management, streaming, fingerprint recommendations, uploads, the copy-only ingest folder, follow notifications, and the rollback-able audit log. Ships an **optional admin UI** that is **not started by default** and enabled via an environment variable (hosts runtime settings, including `SECRET_KEY` rotation).

Full detail: [`server/AGENTS.md`](./server/AGENTS.md).

## Client (summary)
[Tauri 2](https://tauri.app/) ([Rust docs](https://docs.rs/tauri/2.11.3/tauri/)) with a React 19 + TypeScript frontend (Vite) and a Rust native core. Cross-compiles to **desktop and Android** at minimum, with **best-effort iOS**. Handles playback, offline cache, login, library browse/search, playlists, uploads, opt-in metadata editing, follow/notifications, and permission-gated admin/manager + audit/rollback UI.

Full detail: [`app/AGENTS.md`](./app/AGENTS.md).

## Project-Wide Conventions
- **Ingest = copy, never move/delete** the source.
- **Every mutating action is audited and rollback-able.**
- **gRPC primary, REST fallback** — keep them at feature parity on both server and client.
- **Server enforces permissions;** clients must not assume client-side checks are sufficient.
- Keep the per-component `AGENTS.md` files updated as features land.

## Documentation Maintenance
Docs are layered by domain ownership:
- [`server/AGENTS.md`](./server/AGENTS.md) owns server status; [`app/AGENTS.md`](./app/AGENTS.md) owns client status.
- This root file owns the high-level/cross-cutting picture.
- [`README.md`](./README.md) is the public-facing mirror.

**Rule:** when work changes a domain, update that domain's `AGENTS.md` first, then propagate any high-level or status change up to this file and to [`README.md`](./README.md). Keep all **Status** sections in sync.

## Status
Server through **Phase 7 (Metadata & Artwork)**: async bootstrap, gRPC + REST transports at feature parity, Postgres pool + portable schema, argon2 auth + session tokens + tier-inheriting `Identity`, gRPC proto files (`auth.proto` + `library.proto` + `playlist.proto` + `upload.proto`) under `server/proto/`, `LibraryService` with Manager+ gating + before/after audit writes, `ScanService` indexing on-disk files via `lofty`, `StreamingService` + `GET /tracks/:id/stream` with full RFC 7233 byte-range semantics (200/206/416), safe path resolution under `LIBRARY_PATH`, and `Accept-Ranges`/`Content-Range`/`Last-Modified` headers, and `PlaylistService` covering CRUD + 1-based contiguous track ordering (append, insert-with-shift, remove-with-shift, forward/backward reorder via tx-safe negate-shift) gated to playlist owner-or-`Manager+` with per-mutation `playlist.*` audit entries, surfaced over both `music.playlist.v1` gRPC and a `/playlists/...` REST tree.  Phase 6 adds a shared `tag` module for metadata extraction (`TagInfo` + `read_tags`), an `Organizer` for `Artist/Album/Track.ext` copy-only library layout with `sanitize()` path components, an `IngestService` orchestrating read-tags → upsert artist+album → copy → `ScanService.index_file`, a background `notify` folder watcher with per-path debounce (300 ms settle + in-flight `HashSet`), and two REST endpoints (`POST /upload` multipart with 500 MiB cap + `POST /ingest/scan`) gated to Manager+, plus archive ingest (`services::archive` extracting zip + `.tar`/`.tar.gz`/`.tar.bz2`/`.tar.xz` with zip-slip-guarded `safe_join`; ISO/CD recognised but stubbed) wired into `POST /upload` and a client-streaming `music.upload.v1` `UploadService` gRPC (`UploadInfo` + chunked bytes, 500 MiB cap). Phase 7 adds opt-in metadata editing (`MetadataService` wrapping audited `update_track` with optional `lofty` tag write-back gated by `WRITE_TAGS`), automatic album artwork (`ArtworkService` + `CoverArtSource` trait; `CoverArtArchive` resolves a MusicBrainz release MBID then pulls the Cover Art Archive front cover, caches it under `ARTWORK_PATH`, and updates the album `cover_path` via audited `update_album`, gated by `FETCH_ARTWORK`), surfaced over gRPC (`EditTrackMetadata` + `FetchAlbumArtwork`) and REST (`PATCH /tracks/:id/metadata` + `POST /albums/:id/artwork`). Client through **Phase 9 (Metadata Editing)**: Tauri 2 + React 19 + Vite shell; embedded `sqlx` SQLite cache; gRPC-primary / REST-fallback `ServerClient` over the server's `auth.proto` + `library.proto` + `upload.proto`; `AuthManager` over OS-keychain (desktop) / app-private file (Android) secure store handling `SECRET_KEY` + username/password; `LibraryService` returning `MergedArtist`/`MergedAlbum`/`MergedTrack` rows with per-row `downloaded` flags + a `LibraryView { source, items, total? }` wrapper that falls back to the SQLite cache on transport errors or missing credentials; six `library_*` Tauri commands + mirrored TS bindings + `/library`, `/artists/:id`, `/albums/:id`, `/search` routes. Phase 4 adds a custom **`media://` URI-scheme protocol** that, per request, serves a downloaded local file with full RFC 7233 byte-range semantics or proxies the server's `GET /tracks/{id}/stream` with the auth header injected + `Range` forwarded (prefer-local-else-stream resolution lives in Rust; the session token stays out of the webview), a `player_media_url` command for the platform-correct URL, and a Zustand player store + persistent `PlayerBar` (queue, shuffle, repeat, seek, volume, click-to-play from Album/Search) wired into the OS Media Session API for media-key/lock-screen control. Phase 5 adds a **sync engine**: transport get-by-id (artist/album/track, 404→prune) + full playlist ops on both gRPC + REST, a `pending_ops` offline-edit outbox replayed FIFO on reconnect (local-id remap; server-authority conflict resolution), content-hash reconcile of cached entities (`updated_at` isn't on the wire) preserving client-owned download fields, and a missing-file prune — surfaced via `sync_now`/`sync_pending_count`/`sync_enqueue_op` and auto-triggered on online-regain + window focus. Phase 6 adds a **download manager** (`DownloadManager`) streaming track files from `GET /tracks/{id}/stream` into `<final>.part` with **Range-based resume** on `206`, atomic rename, single/album/playlist batches (already-downloaded tracks skip), best-effort **client-side Cover Art Archive** cover fetch (server exposes no cover-byte endpoint) writing `album_art.local_cover_path`, a delete flow that prunes files + cache rows + empty-album covers, storage accounting (`downloads_storage_usage`), a configurable downloads root + Wi-Fi-only toggle persisted in a new `settings` table (migration `0003`), progress via `download-progress` Tauri events, and a second **`cover://` URI-scheme protocol** serving local covers to the webview — surfaced via nine `download_*`/`downloads_*` Tauri commands + a `/downloads` route (storage, dir, Wi-Fi toggle, in-progress bars, per-track remove) plus per-track Download/Remove buttons on the Album page and album covers in the Artist grid + now-playing bar. Phase 7 adds **playlist management**: a `PlaylistService` orchestrating list/get/create/rename/delete/add/remove/reorder with a single online/offline rule per mutation — server-known ids push to the server then mirror into the cache; `Transport` failures, `AuthNotConfigured`, or client-minted `local:` placeholder ids (offline-created, create op still queued) instead append a typed `PendingOpKind` to the Phase 5 outbox **and** apply the change optimistically to the cache (read → splice → renumber 1..N → transactional `replace_playlist_tracks`) so the UI reflects the edit immediately; `local:` delete also prunes dependent queued ops; `PlaylistDetailView.entries` reuse `library::MergedTrack` so the player queues them directly — surfaced via eight `playlist_*` Tauri commands + `/playlists` and `/playlists/:id` routes (create, rename, delete, ▶ Play-all, ⬇ Download-all, inline track-search-add, per-entry ↑/↓ reorder + remove, owner-or-Manager+ edit gating, `unsynced` badge for `local:` ids). Phase 8 adds **uploads**: a new `upload_file` Tauri command reads a local path from a native file-picker dialog (`tauri-plugin-dialog`), loads the bytes in Rust, and pushes them via gRPC client-streaming `UploadService.Upload` (first `UploadInfo{filename}`, then 64 KB `chunk` messages) or REST `POST /upload` multipart fallback; Manager+ gated at both client UI and server. The `Upload` route supports single-audio and archive (zip/tarball) file picker, a directory picker (folder-walking deferred), a drag-drop drop zone, and status feedback (success/error/reset). `Sidebar` gains a Manager+-gated Upload nav item. **Uploads v2** then replaces the disk-only chunked upload with a **DB-backed, session-oriented** subsystem: `init` declares a whole session (a list of files, each with a recombined-file hash + per-chunk hashes that the server re-verifies on arrival — a corrupt chunk is rejected), and a completed session produces a queryable **report** (`GET /uploads/:id`) detailing every ingested song/album/artist. Sessions are listable (`GET /uploads`, own-by-default / admin-sees-all + `?user_id=&state=` filters), cancellable (`POST /uploads/:id/cancel`, which cleans staged chunks off disk), limited to **one active upload per user**, and broadcast **live** over gRPC server-streaming (`StreamUploads`) with a REST **WebSocket** fallback (`GET /uploads/stream`) — both filtered so a listener only sees their own uploads (admins see all). The client moves upload state into a global store (survives tab switches) with a global completion listener that refreshes the library (fixing "uploads succeed but never appear"), enforces one-at-a-time in the UI, and adds an `/uploads` reports route (list + per-file/per-chunk detail + ingest report). Phase 9 adds **opt-in metadata editing** (Manager+) with **no server change** (the Phase-7 `EditTrackMetadata` gRPC / `PATCH /tracks/:id/metadata` REST already cover it): a `MetadataEdit` transport type + `edit_track_metadata` across gRPC/REST/`ServerClient`/`AuthManager`, a `LibraryService.edit_track_metadata` that pushes to the server then mirrors the change into the offline cache for downloaded items (reconcile on next sync already covered by the Phase-5 track content-hash), the `library_edit_track_metadata` command, and a reusable `MetadataEditor` modal — **single-track** (mobile bottom-sheet: title/track#/disc#/year + raw-JSON) and **batch** (desktop table + "set disc/year for all" + "number sequentially"; only changed rows saved) — wired into the Album route (per-track ✏ + desktop "Edit tags") and the upload-report ingest list (on-upload), sending only the touched fields. A follow-on **artwork-upload extension** (server + client) lets managers upload their own **album cover** + **artist images** (not just CAA auto-fetch): the server adds `artists.image_path` (migration + `Artist` proto/DTO), decouples `ArtworkService` from `FETCH_ARTWORK`, and serves REST `POST /albums/:id/cover`, `POST /artists/:id/image`, `GET /artists/:id/image` (binary uploads are REST-only); the client reads the picked file natively (content-type sniffed, 16 MiB cap), pushes it, extends the `cover://` scheme to proxy artist images, and adds a reusable `ImageUploader` modal reachable from the Album hero cover + a new Artist-route image hero. **Image optimization** then makes artwork load fast: the server downscales + re-encodes cover/artist images to small JPEGs (max 800px, q82) into a `<ARTWORK_PATH>/.optimized/` cache, generated **on startup, on upload, on a 6h schedule, and on-demand** at serve time for any not-yet-optimized image (pristine sources untouched; mtime-based freshness re-optimizes after a re-upload). A companion client fix replaces every artwork `<img>` with a `FallbackImg` that retries when its `src` changes, so a re-uploaded cover/image now appears immediately (the cache-bust bump) instead of staying hidden until a manual page refresh, and the uploader shows the new image in-place. The upload client also sends up to **4 chunks of a file in parallel** (`buffer_unordered`, each future owning an `Arc`-shared slice so the spawned job stays `Send + 'static`), since the server already accepts concurrent chunks safely (per-chunk verify + atomic received-count recompute + single-finalizer election) — no server change. **Artwork load is then made instant** with a low-res cache + blur-up: the server adds a tiny ~64px JPEG variant (served via `?lowres=1`), and the client's `cover://` proxy gains a bounded in-memory byte cache (custom-scheme responses aren't reliably WebView-cached, so without it every render re-hit the server) plus a `BlurUpImage` that shows the blurred low-res instantly and cross-fades the full image in — eliminating the 1–2 s blank-then-pop-in. **Background uploads** then stop **instantly failing** when the Android app is backgrounded or the screen is locked: a native `dataSync` **foreground service** (Kotlin `UploadService` + `UploadServicePlugin`, bridged from the Rust `upload_session` plugin and driven by the upload job's start/update/stop with an RAII stop-guard) keeps the upload process + network alive behind a **persistent, non-dismissible progress notification**, holding a partial **wake lock** (CPU on with the screen off) + a **WiFi lock** (radio up with the screen off) for the duration — the canonical fix for the previously-deferred "FGS for uploads surviving process death". **Uploads are then pausable**: a new `paused` session state (server + client) the user can pause/resume from the UI, plus an **auto-pause** when chunk uploads stall/fail for ≥1 minute (the client retries indefinitely with backoff instead of failing, and resumes automatically when a chunk lands again) — surfaced server-side as REST `POST /uploads/:id/{pause,resume}` + gRPC `PauseUpload`/`ResumeUpload` plus an **authoritative server-side stall sweeper** (a 20 s background task that marks any upload idle ≥1 min as `paused`, so the server reflects a stall even when the client's own pause can't be delivered — its network is down or it was killed), and client-side as a per-job control (stall monitor + pause/cancel atomics) with Pause/Resume affordances on the upload card and reports. Uploads are also **crash-safe resumable**: at upload start each Android `content://` source is **copied into app-private staging** (a SAF grant is lost on force-quit, so the original URI can't be re-read), and a local resume manifest (`upload_id` + staging key + each file's staged path + whole-file hash) lets the client **auto-resume** after a process kill — on next launch it re-hashes the staged files (cancelling the session as *corrupted* if any changed), fetches the server's received-set, and uploads only the missing chunks by reading them from the staged copies (no re-hash-then-restart, no re-upload, and whole files never sit in RAM). Staging is cleaned up on every terminal outcome (orphans swept on startup). The Android app also declares **`MANAGE_EXTERNAL_STORAGE`** ("All files access") + granular media permissions, with native check/request commands and an in-app prompt that opens the system grant screen — so music can be read/uploaded from anywhere in shared storage. **Metadata merge (Phase 10)** then lets managers collect cross-spelling duplicates into one entity: new `artist_aliases`/`album_aliases` tables preserve **every** spelling (with its inferred/declared language + an `is_primary` flag) and a `tracks.is_single_release` flag, while a `PRIMARY_LANGUAGE` env var picks which spelling is shown as the canonical `name`/`title` (e.g. English `YUQI` over Korean `우기 ((여자)아이들)`). `LibraryService` gains destructive, audited `merge_artists`/`merge_albums` (re-point the duplicate's albums/tracks/followers onto the survivor, copy its spellings, delete it, recompute the display name), `move_track` (re-point album + flag single release + prune the emptied source album), `set_track_single_release`, and full alias CRUD — surfaced over gRPC + REST (`POST /{artists,albums}/:id/merge`, `POST /tracks/:id/move`, alias endpoints) with `AliasInfo`/`is_single_release` on the entity payloads (aliases on single-entity reads only). The client threads the new fields through transport/`Merged*`/`ipc.ts`, adds server-first `get_artist`/`get_album`, and ships a reusable `EntityPicker` (merge + move-to-album) + an `Aliases` "Also known as" editor on the Artist/Album routes, with a **SINGLE** badge + per-track single-release toggle/move (all Manager+, offline-disabled). Verified end-to-end against Postgres. **Follows & Notifications (Phase 10, server-side)** then lands the new-release alert pipeline: a `notifications` table (migration `20260601000000`) + `NotificationRepo`, and a new `NotificationService` owning follow/unfollow (audited, `SECRET_KEY` rejected), per-user notification reads (list / unread-count / mark-read / mark-all-read), and the new-release **fan-out** — hooked into `LibraryService::create_album` (best-effort, actor-excluded) so a "release" is an album and detection fires once for both manual creates and ingest. Surfaced over gRPC (`music.notification.v1` `NotificationService`) + REST (`/artists/:id/follow`, `/following`, `/notifications…`) at parity, 8 new service tests (104 lib tests). The **client** then consumes it (foreground/polling): the regenerated `music.notification.v1` stubs + gRPC-primary/REST-fallback `follow`/`unfollow`/`is_following`/`list_following` + notification list/unread-count/mark-read across transport→`ServerClient`→`AuthManager` (eight `#[tauri::command]`s), a **Follow** toggle on the Artist route (bearer-user only — `SECRET_KEY` has no user to own a follow), a **Notifications** route (tap-to-open the album + mark-read/all), an unread **badge** on the desktop sidebar + mobile bell, and a `useNotificationsScheduler` that polls the unread feed (online + signed-in, 30 s floor + on focus) → updates the badge + fires an OS notification (`@tauri-apps/plugin-notification`, desktop **and** Android) per newly-seen row (de-duped via a persisted id set; 48 client lib tests). For **delivery while the app is closed**, a native Android **WorkManager** job (`NotificationPollWorker` + a `notify_sync` Rust↔Kotlin bridge) wakes ~every 15 min independent of the app process, polls `/notifications` headlessly (bearer token read from the secure store in Rust, never the WebView), and posts new ones (skipping when foreground). **Instant push (FCM/APNs) is still deferred** — the server has no push transport yet — so the closed-app path is the ~15-min poll (Android; iOS while-closed would need APNs).
